# Helm — One-Shot Prototype Build Brief ("Contour Sheet")

Repo: `C:\Users\xray1\Claude projects\helm` (container form `/mnt/c/Users/xray1/Claude projects/helm`), HEAD `26f9cb3`. Everything below is grounded in the repo as it stands; where this brief says a file does something, it was verified by reading that file at that commit. Do not re-audit; build.

This brief is executed as **two sessions with a hard checkpoint** (section 9). Session 1 is the backend, fixtures, lint scripts and IP sweep, accepted by a scripted REST/WS run of the demo; Session 2 is the console. Each session ends with its own Final report (section 10). Nothing in Session 2 starts until Session 1's checkpoint is green.

**If you are a single session with the budget to continue, do not stop at the checkpoint.** Write the Session 1 report, then start Session 2 in the same session. The checkpoint is a gate (all Session 1 verifications green), not a pause. Only stop there if you cannot make the gate green — then the report says exactly what is red and why.

## 1. Mission

"Finished" means: a person clones the repo, runs `npm install && npm run demo`, opens the URL the script prints (`http://localhost:5173/?match=<uuid>`), and plays the scripted demo match in `devdata/demo/first-five-minutes.md` end to end — survey and deploy, one legal move that returns CLEAR, one illegal move that returns STOP with the rule cited, one countersigned override, one declared attack with an audited seeded roll whose hash verifies in the browser, and a phase advance into round 2 — against the real api-gateway (:3000), rules-engine (in-process) and dice engine (in-process via the gateway) in under ten minutes, with no Docker, Postgres or Python. The console must look authored: a survey-plan aesthetic ("Contour Sheet", section 5) on warm vellum with three ink weights, three typefaces, hairline rules and hatch — no dashboard tiles, no gradients, no emoji, no fantasy iconography. `npm run typecheck` and `npm test` are green at every commit, and the repo contains zero Games Workshop IP, verified by a script that walks the whole repo.

Session 1's "finished" is the same match played by `node scripts/demo-smoke.mjs` over REST and WebSocket with every step printing `PASS` (3.7), plus the gates.

## 2. Read first / non-negotiables

- Read `CLAUDE.md` at the root before touching anything. Its Working Agreement wins over this brief where they conflict. Three conflicts are pre-resolved here so they do not reach the report: (a) CLAUDE.md's Gotchas require updating a service Dockerfile's `--workspace` list when it gains a `@helm/*` dependency — for 3.2 you may edit `services/api-gateway/Dockerfile` by exactly three lines (add `--workspace=@helm/dice-service` to the `npm install` line, and `COPY services/dice-service/package.json services/dice-service/` and `COPY services/dice-service/ services/dice-service/` beside their siblings); no other Dockerfile edits. (b) the IP sweep (section 5) updates CLAUDE.md in the sweep commit: the rule count (20 → 24), the test count, the `Testing` table sentence that names a squad, the console paragraph (the hook it names is deleted in Session 2) and the smoke-test `count:` line. Naming the target game and its publisher in prose is nominative use and stays; what goes is GW-owned creative content (see section 5). (c) `services/vision-service` is not touched at all — no Python runs on the demo path and its docstrings only name the game.
- `npm run typecheck` (tsc --noEmit across all 7 workspaces: `packages/shared-types`, `services/api-gateway`, `services/rules-engine`, `services/state-engine`, `services/dice-service`, `services/voice-service`, `apps/web-console`) and `npm test` (node:test via tsx, `src/*.test.ts` next to code) must pass at every commit. `services/state-engine` is nobody's runtime dependency but it compiles the shared schemas and pins the old enum literals in `reducer.test.ts`; it must stay green through every schema commit. Extend tests for everything you add. No vitest, no jest, no Playwright.
- Stack is frozen: React 18, Vite 5, Tailwind 3, Zustand 4, zod 3, canvas 2D, node:test. No new runtime dependencies anywhere (the console gets `tsx` as a devDependency for tests, nothing else). No Docker, no Postgres, no Python on the demo path.
- Host facts that shape the work: native Windows 11, Node 24.19, npm 12 workspaces, PowerShell primary with Git Bash available; `spawn('npm')` without a shell fails on this Node. Chrome is installed; **Firefox is not** — the Firefox QA run is a known gap declared now, not discovered later. `apps/web-console/Dockerfile` installs the console standalone and will stop building once the console depends on `@helm/shared-types`; it is not on the demo path, do not fix it, list it under Known gaps.
- Zero GW-owned creative content anywhere in the repo — code, fixtures, docs, tests, package.json keywords, service banners, root-level markdown, SQL: no faction, unit, character, weapon, stratagem, mission or battle-size names, no rules text, no lore vocabulary, no iconography. Naming the game ("Warhammer 40,000", "40K") and the publisher as the thing this tool referees is allowed and expected — the README already does. `node scripts/lint-ip.mjs` (section 5) walks the repo root and must exit 0 before the Session 1 final commit and stay 0 through Session 2.
- Small logical commits with clear messages (`feat(gateway): apply MoveUnit and broadcast events`), each leaving both gates green.
- Never claim something works without running it. Every verification line below is a command you run and whose output you quote in the report.
- Schema changes go to `packages/shared-types` first, then every consumer and every test literal, in the same commit. `tsc` will name any literal this brief's lists miss; fix it in that commit, not later.
- Coordinate convention (decide once, everywhere): `Position.x/y` are table inches, origin bottom-left, x 0–60 along the long edge, y 0–44; `tableInches` is a mirror of x/y. Terrain footprints are polygons whose vertices are listed counter-clockwise starting at the bottom-left corner; a rectangle "at (x,y) w×d" means vertices (x,y) (x+w,y) (x+w,y+d) (x,y+d). Add both sentences as a comment on `PositionSchema` in `packages/shared-types/src/events.ts`.

## 3. Backend gaps to close first (Session 1)

Do these in order; each has a test.

**3.1 Shared schema additions** — `packages/shared-types/src/entities.ts`, `events.ts`, `commands.ts`, new `ws.ts`. One commit.
- `ArmySchema` gains `profiles: UnitProfileSchema.array().default([])`. Units keep `profileId`; the console and rules resolve names/stats through `army.profiles`. Because `.default([])` makes `profiles` a required key of the inferred `Army` output type, add `profiles: []` to every `Army` literal in the same commit: `services/api-gateway/src/state-engine.ts:48` (inside `emptyPlayer`), `services/state-engine/src/reducer.ts:138`, `services/state-engine/src/state-manager.ts:239`, `services/rules-engine/src/rules-engine.test.ts:46`, `services/state-engine/src/reducer.test.ts:32`, and any literal in `services/api-gateway/src/state-engine.test.ts` that `tsc` reports.
- `GameSizeEnum` becomes `'skirmish' | 'patrol' | 'standard' | 'grand'`. Move the enum definition into `events.ts` next to `PhaseEnum` (entities.ts already imports from events.ts, so this adds no cycle), `export { GameSizeEnum, type GameSize } from './events'` in `entities.ts`, and make `MatchCreatedSchema.gameSize` (events.ts:27–32, an inline duplicate) use `GameSizeEnum`. Rename every literal in the same commit, mapping `combat_patrol→skirmish`, `incursion→patrol`, `strike_force→standard`, `onslaught→grand`: `services/api-gateway/src/state-engine.ts:65` (fallback becomes `'standard'`), `state-engine.test.ts:38,44,50–52` (the fallback test round-trips `'patrol'` and falls back to `'standard'`), `routes.test.ts:53,57,61`, `services/rules-engine/src/rules-engine.test.ts:62`, `services/state-engine/src/reducer.test.ts:50,68,74`. While there, rename the mission string `'Take and Hold'` in `state-engine.test.ts:38,46` to `'Marker Control'`.
- `DiceRolledSchema` gains `hash: z.string()` (events.ts:134–142). Without it `MatchEventSchema.parse` strips the hash and nothing downstream can verify.
- New `UnitDeployedSchema = BaseEventSchema.extend({ type: z.literal('UnitDeployed'), unitId: z.string().uuid(), from: PositionSchema.nullable(), to: PositionSchema })`, added to the `MatchEventSchema` union; it is the survey-commit event (3.7) and touches no status flags.
- `TerrainPieceSchema.name: z.string().optional()` and `ObjectiveSchema.label: z.string().optional()` so the section 4 fixtures carry their names; `MatchStateSchema.deploymentZones: z.array(z.object({ playerId: z.string(), rect: z.tuple([z.number(), z.number(), z.number(), z.number()]) })).optional()` (optional, not defaulted, so no state literal cascades; the console falls back to y 0–12 / 32–44 when absent).
- New `packages/shared-types/src/ws.ts`, exported from `index.ts`: `ClientMessageSchema` = discriminated union on `type` of `subscribe{matchId}` | `unsubscribe` | `command{command: MatchCommandSchema}` | `ping`; `ServerMessageSchema` = `welcome{clientId}` | `state_update{state: MatchStateSchema}` | `command_result{commandId: string, legalityResult?: LegalityResultSchema, events?: MatchEventSchema.array(), error?: string}` | `event{event: MatchEventSchema}` | `pong` | `error{message}`. `legalityResult`, `events` and `error` are all optional because a rejected envelope carries only `commandId` and `error`. This pins the dialect the gateway already speaks.
- Add the coordinate comment to `PositionSchema` (section 2).
- Verify: `npm run typecheck` (all 7 workspaces), `npm test`; add `ws.test.ts` (3 cases: command envelope parses, `command_result` with only `commandId`+`error` parses, unknown type rejected). `schemas.test.ts` still passes with the renamed enum.

**3.2 applyCommand does work** — `services/api-gateway/src/state-engine.ts` (`StateEngine.applyCommand`, lines 133–159 today).
Return `{ success: boolean; newState?: MatchState; events: MatchEvent[]; roll?: DiceRollResult; error?: string }` and switch on `command.type`:
- `MoveUnit`: find the unit across `players[].army.units`; if `unit.position === null` return `{success:false, events:[], error:'Unit <label> has no position'}` (`UnitMoved.from` is non-nullable); `from = unit.position`; `distanceMoved = Math.hypot(dx, dy)` on x/y; set `position = destination`, `status.hasMoved = true`, `hasAdvanced = moveType==='advance'`, `hasFallenBack = moveType==='fallback'`, `remainedStationary = false`; recompute `status.isInEngagement` for every unit on the table (true when an enemy unit with a position lies within `ENGAGEMENT_RANGE_INCHES = 1`); emit `UnitMoved {unitId, from, to, moveType, distanceMoved}`. Missing unit → `{success:false, events:[], error:'Unit <id> not found'}`.
- `DeclareAttack`: set attacker `status.hasShot = true`; emit `AttackDeclared {attackerUnitId, defenderUnitId, weaponIds}`.
- `RollDice`: a module-level `const dice = new AuditableDice()` (it is an instance method, dice-engine.ts:58); `const roll = dice.roll(diceCount, 6)`; `results = roll.results.map(d => d + modifiers >= targetValue ? 1 : 0)`; emit `DiceRolled {rollId: roll.id, purpose, diceCount, dice: roll.results, results, seed: roll.seed, hash: roll.hash}`; return `roll` in the result so tests can call `dice.verify(roll)` directly. The hash is `sha256(seed + JSON.stringify(results))` (dice-engine.ts:153); the console recomputes exactly that string.
- `UseStratagem`: `player.cp = Math.max(0, cp - cpSpent)`; emit `StratagemUsed`.
- `AdvancePhase`: `const r = this.advancePhase(matchId)`; forward `r.event` — do not record a second phase event.
- `ScoreObjective`: `player.vp += points`; emit `ObjectiveScored {objectiveId, scoringPlayerId, points, round}`.
- `advancePhase(matchId)` now returns `{ state: MatchState; event: MatchEvent } | null` and records `PhaseAdvanced {from, to, round}` itself (it is the only place that does). Round semantics change to the tabletop convention: the active player swaps at every morale→command wrap, but `round` increments only when the wrapping player is `players[1]` (so R1/A → R1/B → R2/A). Update `state-engine.test.ts:63–71`: destructure `{ state }` from each call, expect `round === 1` and `activePlayerId === 'p2'` after seven advances, then seven more → `round === 2`, `'p1'`. `services/state-engine/src/command-to-events.ts` keeps its own arithmetic and is **not** mirrored on this point; say so in the report. Update `routes.ts` callers of `advancePhase` for the new return shape.
- Status reset on the morale→command wrap: reset all eight flags exactly as `services/state-engine/src/reducer.ts:222–246` does (`hasMoved/hasAdvanced/hasFallenBack/hasShot/hasCharged/isInEngagement/isBattleShocked = false`, `remainedStationary = true`), then immediately recompute `isInEngagement` from positions so units that are still within 1" stay flagged. The reducer is the mirror for the eight flags; the recompute is gateway-only. Nothing resets on pre_game→command (not a wrap).
- `recordEvent(matchId, payload)` builds a real `MatchEventSchema` member (`id: uuidv4()`, `matchId`, `timestamp` ISO, `sequence` = log length + 1, `playerId`, plus the payload), runs `MatchEventSchema.parse` and stores the parsed value; it returns the event. Delete the ad-hoc `army_submitted/command_executed/phase_advanced/override_applied` names: `submitArmy` records `ArmySubmitted`, `createMatch` records `MatchCreated`, the deploy route records `UnitDeployed`, overrides record `OverrideApplied`, illegal commands record `IllegalActionBlocked`.
- Tests that cannot survive by line edits are **rewritten, not updated**: `state-engine.test.ts:91–102` (old `applyOverride(id,'legality-1')` signature and ad-hoc event names) is replaced by the override test in 3.6; `routes.test.ts:56–91` (posts `{unitId:'u1', distance:3}` with no destination, a non-uuid id, and expects `override {legalityResultId:'lr-1'}` to succeed) is replaced by the lifecycle test in 3.7.
- Dice import: `import { AuditableDice } from '@helm/dice-service/src/dice-engine'` (never the package root — it calls `app.listen`). Add `"@helm/dice-service": "*"` to `services/api-gateway/package.json`, `"@helm/dice-service/*": ["../dice-service/*"]` to the gateway `tsconfig.json` paths (lines 17–20), and the three Dockerfile lines from section 2(a).
- Verify: extend `state-engine.test.ts` — MoveUnit relocates one unit and sets the four flags; distance (0,0)→(3,4) is 5; MoveUnit on a null-position unit fails with the stated error; RollDice event has a 64-hex `seed`, a 64-hex `hash`, and `new AuditableDice().verify(result.roll)` is true; the eight statuses reset on the wrap and `isInEngagement` is recomputed; UseStratagem deducts CP and floors at 0; AdvancePhase via command records exactly one `PhaseAdvanced`; every recorded event passes `MatchEventSchema.safeParse`.

**3.3 Rules resolve against the real state** — `services/rules-engine/src/condition-evaluator.ts`, `rules-engine.ts`, `default-rules.ts`, `rules-engine.test.ts`.
- `extractFieldValue` (condition-evaluator.ts:71–95): add `if (fieldPath.startsWith('command.')) return navigateObject(context.command, fieldPath.slice(8))` before the `unit.` branch. Leave the `unit.` branch as it is — it navigates `context.actingUnit` before `additionalFields` are consulted, so `unit.profile.movement` resolves only if the profile is on the acting unit object (next bullet).
- `buildRuleContext`: acting unit from `command.unitId ?? command.attackerUnitId`, target from `command.targetUnitId ?? command.defenderUnitId`; resolve `actingProfile` from the acting player's `army.profiles` by `profileId` and set `context.actingUnit = { ...unit, profile: actingProfile }` (a shallow copy carrying `profile`; never mutate state). `distance` = explicit `command.distance` → else acting position to `command.destination` → else acting-to-target centre distance. `playerCP` from the player whose id is `command.playerId`. `additionalFields`: `'army.totalPoints'` (acting player's army), `moveExcess = distance - profile.movement`, `advanceExcess = moveExcess - 6`, `weaponRange` = range of the weapon in `command.weaponIds[0]` if present, else the acting profile's first ranged weapon, `rangeExcess = distance - weaponRange`. Each computed field is set only when its inputs exist (no NaN).
- Explanation builder (rules-engine.ts, the blocked branch near line 72): `LegalityResult.explanation` for a blocked command becomes the blocking rule's `explanation` template with `{field}` placeholders replaced through `extractFieldValue`, numbers formatted `toFixed(1)`; `suggestedFix` is interpolated the same way and stays a separate field. No `Name: … (fix)` prefix. Adjust the one assertion in `rules-engine.test.ts` that matches the old format, if any.
- `default-rules.ts` renames (data only): `unit.status.inEngagement→isInEngagement`, `advanced→hasAdvanced`, `fellBack→hasFallenBack`, `unit.lastActionType eq 'normal_move'→command.moveType eq 'normal'`, `'fall_back'→'fallback'`, `'advance'→'advance'`. **Delete** the permissive `movement_advance_distance` (`distance lte 10`, lines 108–129): it contradicts the M+6 cap below. Add five blocking rules (`isLegal:false`): phase `movement`: `movement_exceeds_m` [command.moveType eq 'normal', unit.profile.movement gt 0, moveExcess gt 0] explanation `Exceeds M by {moveExcess}`, suggestedFix `Move at most M inches or Advance`; `movement_advance_too_far` [command.moveType eq 'advance', advanceExcess gt 0] explanation `Advance exceeds M+6 by {advanceExcess}` (an advance is legal up to M+6 inclusive); `movement_fall_back_too_far` [command.moveType eq 'fallback', moveExcess gt 0] explanation `Fall back exceeds M by {moveExcess}`; `movement_already_moved` [unit.status.hasMoved eq true] explanation `Unit has already moved this phase`; phase `shooting`: `shooting_out_of_range` [rangeExcess gt 0] explanation `Out of range by {rangeExcess}`, suggestedFix `Choose a target within range or move closer`. The existing `shooting_range_check` (`distance gte 0`) stays as the permissive rule cited on CLEAR. Rule count becomes 20 − 1 + 5 = **24**: update `rules-engine.test.ts:102`, `routes.test.ts:45–46`, and `CLAUDE.md` (three places: the "20 default rules" line, the smoke-test comment, the Testing paragraph).
- Characterization test (rules-engine.test.ts:228–259): the test sorts field names, so both arrays are alphabetical. `RESOLVED = ['advanceExcess','army.totalPoints','command.moveType','distance','moveExcess','playerCP','rangeExcess','unit.profile.movement','unit.status.hasAdvanced','unit.status.hasFallenBack','unit.status.hasMoved','unit.status.isInEngagement']`; `UNRESOLVED = ['stratagem.usedThisTurn','targetUnit.owner','unit.selectedWeapon.type','unit.status.hasCasualties','unit.status.hasLOS','unit.status.wasCharged','unit.type']`. Add to `makeState` a profile (`movement: 6`, one ranged weapon `range: 24`) in `army.profiles` whose id is the fixture units' `profileId`.
- New `default-rules.test.ts` (8 cases, each builds a state in the named phase with a profile M 6): normal 7" → blocked `movement_exceeds_m`, explanation `Exceeds M by 1.0`; normal 6.0" → legal, `movement_distance_limit` in `appliedRules`; `isInEngagement:true` + normal → blocked `movement_engagement_range_block`; `hasMoved:true` → blocked `movement_already_moved`; advance 13" → blocked `movement_advance_too_far` (`Advance exceeds M+6 by 1.0`), advance 12" → legal; fallback 7" → blocked `movement_fall_back_too_far`, fallback 6" → legal; `DeclareAttack` in phase `charge` with the attacker's `hasAdvanced:true` → blocked `charge_cannot_charge_if_advanced` (there is no charge command; the acting unit is `attackerUnitId`); `DeclareAttack` in phase `shooting` at 30" with a 24" weapon → blocked `shooting_out_of_range` (`Out of range by 6.0`), at 20" → legal with `shooting_range_check` applied.

**3.4 Validation, id policy and active-player check** — `services/api-gateway/src/routes.ts` (`POST /matches/:id/command`), `websocket.ts` (`handleCommand`, `handleSubscribe`).
Command ids are **client-owned when valid**: keep `body.id` if `z.string().uuid().safeParse(body.id).success`, otherwise fill `uuidv4()`; always server-fill `matchId` (URL / subscription) and `timestamp = new Date().toISOString()`, overwriting whatever the client sent. `command_result.commandId` therefore echoes the client's id, which is what the console keys its pencil rows by. Then `MatchCommandSchema.safeParse` → REST 400 `{error:'Invalid command', issues}`, WS `{type:'command_result', commandId, error:'Invalid command'}`. Reject `playerId !== state.activePlayerId` for every type except `ApplyOverride` and `QueryRule` (REST 400 / WS `command_result{error:'Not the active player'}`). `handleSubscribe`: unknown match → `{type:'error', message:'Match not found'}` instead of a silent subscription. Flow for a valid command: legality check → if illegal: record `IllegalActionBlocked` (3.5), stash in `blocked` (3.6), reply `command_result{commandId, legalityResult, events:[blocked]}`; if legal: `applyCommand` → on `success` reply `command_result{commandId, legalityResult, events}` then broadcast `state_update` and each event; on failure reply `command_result{commandId, error}`.

**3.5 WS push after every mutation** — `websocket.ts`, `routes.ts`, `index.ts`.
Make `broadcastStateUpdate` public and add `broadcastEvents(matchId, events)`; add `notify?: { state(matchId, state): void; events(matchId, events: MatchEvent[]): void }` as an optional third argument to `createRoutes` (tests may omit it) and pass the WS server from `index.ts`. After `/command` (legal), `/advance-phase`, `/army`, `/deploy`, `/override`, `/end` and `/matches` creation, call both. On an illegal command (REST and WS) record and broadcast `IllegalActionBlocked {attemptedCommandType, ruleId, explanation, suggestedFix}` whose event `id` **is** `legalityResult.id` (already a uuid, routes.test.ts:79), so client and server correlate on one value. REST `/command` returns `{...legalityResult, applied:true, newState, events}` on legal and 400 `{...legalityResult, applied:false, events:[blocked]}` on illegal.

**3.6 Override re-applies** — `state-engine.ts`, `routes.ts`.
Keep `blocked: Map<legalityResultId, {matchId, command}>` on the engine. `applyOverride(matchId, legalityResultId, {reason, initials})` applies the stored command via `applyCommand` with `skipLegality`, records `OverrideApplied {originalBlockedEventId: legalityResultId, reason, approvedBy: initials}` and returns `{success, newState, events}`. `POST /api/matches/:id/override {legalityResultId, reason, initials}` validates `reason.length >= 12` and `/^[A-Za-z]{3}$/` on initials (400 with `issues` otherwise), unknown match → 404, unknown id → 400 `{error:'No blocked command <id>'}`, success → 200 `{success:true, newState, events}` and broadcasts. `POST /advance-phase` accepts optional `{override:{reason, initials}}` and, when present, records `OverrideApplied` **after** `PhaseAdvanced` with `originalBlockedEventId` = that `PhaseAdvanced` event's id (the schema field is a required uuid and a phase advance has no blocked event; this is the stated convention). New test in `state-engine.test.ts` (replaces lines 91–102): sequence numbers stay monotonic across `MatchCreated`, `PhaseAdvanced`, `ArmySubmitted`; a blocked MoveUnit re-applied through `applyOverride` moves the unit and logs `OverrideApplied` referencing the legality id; unknown id fails.

**3.7 Match setup, deploy, listing, demo** — `routes.ts`, `state-engine.ts`, `scripts/demo.mjs`, `scripts/demo-smoke.mjs`.
- `POST /api/matches` accepts `{player1Id, player2Id, player1Name?, player2Name?, gameSize?, mission?, armies?: Army[] (length 2, `playerId` matching), terrain?: TerrainPiece[], objectives?: Objective[], deploymentZones?}` (each zod-parsed; 400 with issues). Armies go through `submitArmy` plus `player.army.profiles/faction/detachment/totalPoints` copied from the submitted army.
- `POST /api/matches/:id/deploy {positions:[{unitId, position}]}` — the survey-commit mutation. Valid only while `phase === 'pre_game'` (400 `{error:'Deploy only in pre_game'}` otherwise); **no active-player check**; sets only `unit.position`; touches no status flag; records one `UnitDeployed {unitId, from, to}` per entry; broadcasts; returns `{newState, events}`. `submitArmy` is unchanged (it filters out units already present, state-engine.ts:109–113, so re-posting armies cannot move anything — that is why this route exists).
- `GET /api/matches` → `{matches: [{id, round, phase, activePlayerId, players:[{id,name}], updatedAt}]}` newest first.
- `POST /api/matches/demo` reads the section 4 fixtures with `fs` (root from `process.env.HELM_DEVDATA ?? path.resolve(__dirname, '../../../devdata')`; the gateway runs as CommonJS under tsx so `__dirname` exists), copies each roster's `profiles` into its army, and returns the seeded `MatchState`. With `HELM_DEMO=1` the gateway seeds one demo match at boot and logs `DEMO http://localhost:5173/?match=<id>`.
- `POST /api/matches/:id/end {reason?: 'victory_points'|'resignation'|'draw'}` → `isActive:false`, records `MatchEnded {winnerId: higher vp or null on a tie, finalScores: {[playerId]: vp}, reason (default 'victory_points')}`, broadcasts. Small; needed by MatchSummary (Session 2, cut-first there but the route is cheap here).
- Root `package.json` gains `"demo": "node scripts/demo.mjs"`. `scripts/demo.mjs`: `const npm = process.platform === 'win32' ? 'npm.cmd' : 'npm'`; `spawn(npm, ['run','dev:api'], { shell: process.platform === 'win32', env: {...process.env, HELM_DEMO:'1'}, stdio:['ignore','pipe','pipe'] })` and `spawn(npm, ['run','dev','--workspace=@helm/web-console'], …)`; pipe both stdouts through; readiness = `/DEMO (http:\/\/\S+)/` on the gateway's output and `/Local:\s+(http:\/\/\S+)/` on Vite's; when both have matched print `OPEN <demo url>` once; on `SIGINT`/`SIGTERM`/`exit` tear both children down (`child.kill()`; on win32 `spawn('taskkill', ['/pid', String(child.pid), '/T', '/F'])` because the shell wrapper would otherwise orphan node).
- `scripts/demo-smoke.mjs` — **Session 1's acceptance test.** Against a running `HELM_DEMO=1` gateway (or it spawns one on a free port), using Node's `fetch` and the hoisted `ws` package, it plays the section 4 script: deploy B5 → (15,36); advance to `command` then `movement`; MoveUnit A1 → (20,14) as `side-a` → 200 legal; MoveUnit A2 → (30,17) → 400 `ruleId:'movement_exceeds_m'`, `explanation:'Exceeds M by 1.0'`; the same command as `side-b` → 400 `Not the active player`; override with reason `Terrain step agreed at table`, initials `REF` → 200 and A2 at (30,17); advance to `shooting`; DeclareAttack A3→B1 with the Repeater Cannon id → 200; RollDice `{purpose:'hit', diceCount:4, targetValue:4}` over the WebSocket with a client-supplied uuid → `command_result.commandId` equals it and `events[0].hash === createHash('sha256').update(seed + JSON.stringify(dice)).digest('hex')`; four advances → `round 1`, `activePlayerId 'side-b'`; six more → `round 2`, `'side-a'`. One `STEP n PASS|FAIL <detail>` line per step, exit 1 on any FAIL. Also print the equivalent curl lines in `devdata/demo/first-five-minutes.md` for humans.
- Verify: `routes.test.ts` gains list; demo seed (2 players, 6 units each with positions, 12 profiles across the two armies, 4 objectives, 11 terrain pieces, phase `pre_game`, `deploymentZones` present); deploy (moves B5, refuses outside `pre_game`, ignores the active player); and the full lifecycle mirroring `demo-smoke.mjs` step for step (create with armies → deploy → advance ×2 → legal move → illegal move with `IllegalActionBlocked` event id equal to `legalityResult.id` → wrong-player 400 → override → attack → roll with hash check → 10 advances → round 2 / `side-a`). New `websocket.test.ts` builds its own server the way `routes.test.ts` does (`createServer(app)` + `createRoutes` + `new MatchWebSocketServer(httpServer, …)` on `listen(0)`) and **never imports `index.ts`** (it listens on :3000 at import): subscribe unknown → error; subscribe known → `state_update`; command envelope with a client uuid → `command_result` echoing it, then `state_update`; malformed envelope → `command_result{error}`.

**3.8 Voice CORS** — no change to voice-service; the Vite dev proxy `'/voice': {target:'http://localhost:3002', rewrite: p => p.replace(/^\/voice/, '')}` goes into `apps/web-console/vite.config.ts` in Session 2 step 5.

## 4. Demo content (Session 1)

All fixtures in shared-schema shape with fixed v4 uuids (generate once, commit; every `id` — profiles, weapons, abilities, units, armies, terrain, objectives — is a literal uuid in the JSON). Delete `devdata/mock-units/space_marines.json`, `devdata/mock-rules/movement_rules.json`, `devdata/mock-terrain/basic_terrain.json` and the three now-empty directories; update the docs that cite them (`CLAUDE.md`, `README.md`, `docs/DATA_MODEL.md` or wherever `grep -rn mock-units` lands).

Profile shape reminders (entities.ts:60–78): `keywords: string[]`, `movement` number, `toughness/save/wounds/leadership/oc` ints, `weapons: Weapon[]` (`type 'ranged'|'melee'`, `range` number or null for melee, `attacks` string, `skill` int, `strength`, `ap`, `damage` string, `keywords`), `abilities: Ability[]` (`phase` nullable, `type 'unit'`), `isBattleline/isCharacter/isEpicHero`, `pointsCosts: { "<modelCount>": points }`. Unit shape: `modelCount = modelsRemaining = models`, `woundsPerModel = profile.wounds`, `woundsRemaining = wounds × models`, `status` all false except `remainedStationary: true`, `label`, `enhancements: []`, `isWarlord` true on each side's character.

**`devdata/rosters/meridian_compact.json`** — `{faction:'Meridian Compact', detachment:'Survey Line', profiles: UnitProfile[]}`. Theme: disciplined, armoured, ranged. Stats M/T/Sv/W/Ld/OC, points, weapons `{name,type,range,attacks,skill,strength,ap,damage,keywords}`:
1. Line Section (battleline, infantry, 5 models) 6/4/3/1/6/2, 75 — Service Rifle r24 A2 BS3 S4 AP0 D1 [rapid_fire_1]; Combat Knife melee A2 WS3 S4 AP0 D1.
2. Entry Team (infantry, 5) 6/4/3/2/6/2, 90 — Scatter Carbine r12 A3 BS3 S4 AP-1 D1 [assault]; Breaching Maul melee A3 WS3 S6 AP-2 D2.
3. Support Weapon Team (infantry, 3) 5/4/3/2/6/1, 70 — Repeater Cannon r36 A4 BS3 S5 AP-1 D2 [heavy]; Combat Knife.
4. Warden (character, 1) 6/4/2/5/5/1, 80 — Service Pistol r12 A1 BS2 S4 AP-1 D2 [pistol]; Halberd melee A5 WS2 S6 AP-2 D2; ability Steady Ranks (friendly infantry within 6" re-roll Leadership tests).
5. Armoured Carrier (vehicle, 1) 12/9/3/10/6/2, 95 — Twin Repeater Cannon r36 A6 BS3 S5 AP-1 D2 [twin_linked]; Hull Ram melee A3 WS4 S6 AP0 D1; ability Transport 10.
6. Cairn Walker (large, 1) 8/9/3/9/6/3, 105 — Lance Cannon r48 A1 BS3 S12 AP-3 D6 [heavy]; Crushing Limbs melee A3 WS3 S8 AP-2 D3.

**`devdata/rosters/ashfall_kindred.json`** — Theme: fast, numerous, melee.
1. Scavenger Pack (battleline, infantry, 10) 6/4/6/1/7/2, 90 — Scrap Pistol r12 A1 BS5 S4 AP0 D1 [pistol]; Cleaver melee A2 WS3 S4 AP0 D1.
2. Ember Runners (infantry, 5) 10/4/6/1/7/1, 70 — Twin Scrap Pistols r12 A2 BS5 S4 AP0 D1 [pistol]; Hooked Blades melee A3 WS3 S4 AP-1 D1.
3. Brute Guard (infantry, 3) 6/6/5/3/7/2, 95 — Great Cleaver melee A3 WS3 S8 AP-2 D2.
4. Cinder Chieftain (character, 1) 6/5/5/6/6/1, 70 — Chieftain's Cleaver melee A5 WS2 S8 AP-2 D2; Scrap Pistol; ability Rally Cry (+1 to charge rolls within 6").
5. Scrap Buggy (vehicle, 1) 12/6/5/6/7/2, 85 — Rattle Gun r24 A6 BS5 S5 AP0 D1 [rapid_fire_2]; Ramming Prow melee A3 WS4 S6 AP0 D1.
6. Smoke Mortar Crew (infantry, 3) 6/4/6/1/7/1, 90 — Smoke Mortar r36 A'd6' BS5 S5 AP0 D1 [indirect, blast]; Cleaver.

Each profile's `keywords` carries exactly one class keyword — `'infantry'`, `'vehicle'` or `'large'` — plus `'battleline'` or `'character'` where stated; the console maps the class keyword to token shape.

**`devdata/armies/demo_a.json`** / **`demo_b.json`** — `{playerId:'side-a', name:'Side A', faction, army: Army}` with `profiles: []` (filled from the roster at seed time — the roster file is the single source of profiles) and six `Unit`s (labels `A1`–`A6`, `B1`–`B6`), positions pre-seeded. A (zone y 0–12): A1 Line Section (20,8), A2 Line Section (30,10), A3 Support Weapon Team (16,6), A4 Warden (24,6), A5 Armoured Carrier (40,6), A6 Cairn Walker (56,5); `totalPoints` 500 = 75+75+70+80+95+105 (two Line Sections, no Entry Team). B (zone y 32–44): B1 Scavenger Pack (18,36), B2 Scavenger Pack (40,36), B3 Ember Runners (30,38), B4 Cinder Chieftain (30,40), B6 Brute Guard (50,38); B5 Scrap Buggy is seeded **once**, at (10,30), deliberately 2" outside its zone to exercise the survey correction; `totalPoints` 500 = 90+90+70+70+85+95 (no Smoke Mortar Crew).

**`devdata/tables/demo_60x44.json`** — `{table:{width:60,depth:44}, deploymentZones:[{playerId:'side-a', rect:[0,0,60,12]},{playerId:'side-b', rect:[0,32,60,44]}], objectives: Objective[], terrain: TerrainPiece[]}`. Objectives (`label`, position, `controlledBy:null, points:0`): O1 (18,14), O2 (42,14), O3 (18,30), O4 (42,30). Terrain as 4-vertex CCW footprints from the bottom-left corner (section 2 convention), `name`, `type`, `height`, `blocksLos`, `providesCover`: T1 Ruined Hall ruin (24,18) 12×8 h6 blocksLos cover; T2 Ruin West ruin (8,16) 8×6 h5 blocksLos cover; T3 Ruin East ruin (44,22) 8×6 h5 blocksLos cover; T4 Woods South forest (14,4) 8×6 h3 cover only; T5 Woods North forest (38,34) 8×6 h3 cover only; T6 Barricade South barricade (27,9) 6×1 h1.5 cover; T7 Barricade North (27,34) 6×1 h1.5 cover; T8 Crater West crater (2,30) 6×6 h0 cover; T9 Crater East (52,8) 6×6 h0 cover; T10 Container Stack West container (9,34) 4×8 h4 blocksLos; T11 Container Stack East (47,2) 4×8 h4 blocksLos. Consequences under the corner convention, all intended: A1 stands in Woods South; B2 stands in Woods North; the B5 correction target (15,36) is clear of T10 (x 9–13) and 3" from B1.

**`devdata/demo/first-five-minutes.md`** — the script, the manual QA script (section 8), and, in a second half, the curl/WS transcript `demo-smoke.mjs` automates:
1. Open the URL; PhaseRail at PRE, SurveySheet shows `registered 0 / 12`. Type `survey` → 2.4s scan line; 11 rows tick as the line crosses them; the B5 row is hazard-hatched and an ink-2 arrow on the plan points from B5 to the nearest edge of zone B. Drag B5 to (15,36) → the row un-hatches; the token stays in pencil (dashed) at (15,36). `Commit survey` → one `POST /deploy` with all twelve positions → `state_update` inks every token; ledger `SURVEY COMMITTED  12 units  sheet 3b0e…`; the interlock is clear, `POST /advance-phase` → command, `R1 / A`, A EDGE bold.
2. `]` → movement. `move a1 to 20,14` → CLEAR, `Movement 6.0 of 6.0 · movement_distance_limit`, A1 re-drawn at (20,14).
3. `move a2 to 30,17` → STOP hatch bar, `Exceeds M by 1.0 · movement_exceeds_m`, 2px stop rule around the plan, rail disabled. Press `O`, reason `Terrain step agreed at table`, initials `REF`, hold Countersign 600ms → OVR row, A2 at (30,17), title block `OVR 1`.
4. `]` → shooting. `shoot b1 with a3` → CLEAR, `Range 30.1 of 36 · shooting_range_check` (A3 (16,6) to B1 (18,36) is 30.07"; Repeater Cannon r36 is A3's first ranged weapon). `roll 4d6` → Dice tab, header `ROLL 0001  seed —  ARMED`, ledger `DICE ARMED 4d6 roll 0001` (the seed is server-generated and cannot exist before the roll); Enter → `ROLLED  seed 1f9a…c2`, four die faces, tally, `sha256 7e31…9d  verified`; ledger `ROLL 4d6  [4 4 1 6]  7e31`.
5. `]` ×4 through charge, fight, morale into `R1 / B` command (B EDGE bold; B's movement phase shows the note `6 units unmoved`, which does not block); `]` ×6 more to `R2 / A`. Each PHASE row carries the elapsed time of the finished phase. Total under ten minutes.

## 5. Design system (Session 2, except the two lint scripts, which are Session 1)

**Ground** — warm vellum. The hex values below are canonical; prose adjectives never override them. Two paper tones, three ink weights, no third paper and no fourth ink.

**Tokens** — `apps/web-console/src/globals.css` (rename from index.css): `:root { --paper:#F2EFE6; --paper-2:#E6E2D6; --ink:#1C1A17; --ink-2:#4A463F; --ink-3:#8C877C; --rule:#C9C3B4; --side-a:#1F4E8C; --side-b:#B4532A; --clear:#2E6B3A; --stop:#C8102E; --hazard:#E0A400; --focus:#0B6E99; --d-text:120ms; --d-draw:160ms; --d-move:240ms; --ease:cubic-bezier(0.2,0,0,1); }`. Export the same twelve hex values from `src/lib/theme.ts` for the canvas. **Canvas paints are limited to those twelve hexes at alpha 1**; the only fill texture is `hatchPattern`; `globalAlpha`, `shadowBlur`, `shadowColor`, `ctx.filter`, `createLinearGradient/RadialGradient/ConicGradient`, `hsl()/hsla()`, `rgba()` and 8-digit hex are banned in the renderer and caught by lint-slop.

**Base typography and native controls** — in `globals.css`, because Tailwind's `fontFamily` is replaced and `font-sans` no longer exists: `body{font:400 14px/20px theme(fontFamily.body);color:var(--ink);background:var(--paper);color-scheme:light}`; `input,textarea{background:transparent;border:0;border-bottom:1px solid var(--ink);border-radius:0;font:inherit;padding:4px 0;color:var(--ink)}`; the command input, match-id input and initials field add `font-family:theme(fontFamily.mono)`; `::placeholder{color:var(--ink-2);font-style:italic}`; `*{scrollbar-width:thin;scrollbar-color:var(--ink-3) var(--paper-2)}`; `:focus-visible{outline:2px solid var(--focus);outline-offset:2px}`; `canvas{display:block;width:100%;height:100%}`; the reduced-motion block below. Nothing else lives in globals.css except the fonts link fallback comment.

**Tailwind** — `tailwind.config.js` replaces (not extends): `colors: {transparent, current, paper, 'paper-2', ink, 'ink-2', 'ink-3', rule, 'side-a', 'side-b', clear, stop, hazard, focus}` bound to the CSS vars; `fontFamily: {display:['"Barlow Condensed"','"Arial Narrow"','sans-serif'], body:['"IBM Plex Sans"','"Segoe UI"','Helvetica','sans-serif'], mono:['"IBM Plex Mono"','Consolas','"Courier New"','monospace']}`; `boxShadow:{}`, `backgroundImage:{}`, `backdropBlur:{}`, `blur:{}`, `dropShadow:{}`, `ringWidth:{}`, `ringColor:{}`, `ringOffsetWidth:{}`, `ringOffsetColor:{}`, `animation:{}`, `keyframes:{}`, `scale:{}`, `opacity:{0:'0',100:'1'}`, `borderRadius:{none:'0', DEFAULT:'0', full:'9999px'}`, `transitionProperty:{DEFAULT:'height,width,color,background-color,border-color,outline-color'}`, `transitionDuration:{DEFAULT:'120ms', text:'120ms', draw:'160ms', move:'240ms'}`, `transitionTimingFunction:{DEFAULT:'cubic-bezier(0.2,0,0,1)'}`. `shadow-*`, `ring-*`, `drop-shadow-*`, `blur-*`, `animate-*`, `scale-*`, `bg-gradient-*`, `backdrop-blur-*`, `rounded-md` and fractional `opacity-*` cannot exist to be used. Delete `tsconfig.app.json`. Delete the `dark` class and `/vite.svg` in `index.html`; title `Helm — Referee Sheet`.

**Type** — one link in `index.html`: `https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@500;600;700&family=IBM+Plex+Mono:wght@400;500&family=IBM+Plex+Sans:ital,wght@0,400;0,500;0,600;1,400&display=swap`. Barlow Condensed (DIN-descended plate lettering; condensed so a 14px letter fits a 22px token; 700 reads as a stamp) for map labels, phase stations, token letters, kind glyphs, round stamp, verdict words — uppercase, 0.06em tracking; sizes 14 (labels/tokens, never smaller), 15 (stations, headings), 28/700 (round stamp), 36/600 (CLEAR/STOP/OVERRIDE). IBM Plex Sans (drawn for technical documentation, shares vertical metrics with Plex Mono) 14px/20px body, 400/500/600, pending text 400 italic ink-2, `font-variant-numeric: tabular-nums` on every numeric column. IBM Plex Mono 13px, `font-feature-settings:'zero'`, for coordinates, distances, seeds, hashes, command input, ids, clocks; readout columns sized in `ch`. Floor 13px everywhere.

**Ink for text** — ink-3 is for hairlines, grid, hatch strokes, ghost linework and strike-through lines only; **no text is ever set in ink-3**, including placeholders, disabled controls, future phase stations and the `UNVERIFIED` label. Every text is ink or ink-2 (ink-2 on paper-2 is ~7:1). Placeholder = ink-2 italic; disabled = ink-2 with a 1px strike-through; future stations = ink-2 weight 500 with a hollow tick; `UNVERIFIED` = ink-2 italic with a 12px `HatchSwatch` (hazard) beside it; struck timeline rows keep ink-2 text under an ink-3 strike line; in `pre_game` the plan's linework is ink-3 but its numerals and labels stay ink-2. lint-slop flags `text-ink-3` and `placeholder-ink-3`.

**Rhythm and rules** — 4px base grid; paddings 8/12/16/24; row height 20 or 28; grid regions separated by `1px solid var(--rule)` borders on cells (never gaps, never boxes); 2px `ink-2` hairline before PHASE rows; plan border 1px ink, 2px `stop` while a STOP stands. Hatch: `hatchPattern(color, pitch)` in the renderer (8x8 offscreen canvas, 45°, 1px ink stroke, pitch 3/4/6) and two SVG data-URI classes `.hatch-hazard` (ink on hazard, 4px) and `.hatch-zone` (ink-3 on paper-2, 6px). Motion: only the three durations and one easing; `@media (prefers-reduced-motion: reduce) { :root { --d-text:0ms; --d-draw:0ms; --d-move:0ms } }` and the renderer reads a `reducedMotion` flag to skip sweeps. Delete `.animate-pulse` and index.css lines 54–95.

**Glyphs** — every glyph is an inline SVG from `Glyph.tsx` (`<Glyph name size>`), constructed in the same hand as the map linework: `viewBox` `0 0 12 12` or `0 0 16 16`, `fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="square" stroke-linejoin="miter"`, every stroke coordinate on the half-pixel grid, no curve except the mic capsule, no fill except the OVR mark. The set is closed; adding a glyph is a design change, not a convenience. 12px: `prompt` `M3.5 2.5 L7.5 6 L3.5 9.5`; `caret` `M2.5 4.5 L6 8 L9.5 4.5` (collapsed state adds `transform="rotate(-90 6 6)"`); `tick` `M2.5 6.5 L5 9 L9.5 3.5` (CLEARED, placed, checklist); `tick-hollow` `M3.5 3.5 H8.5 V8.5 H3.5 Z` (future phase station); `ovr` `<rect x="2" y="2" width="8" height="8" fill="currentColor" stroke="none"/><path d="M2 2 L10 10" stroke="var(--paper)"/>` (OVR-pending and OVR, in `stop`). 16px: `copy` `M5.5 5.5 H13.5 V13.5 H5.5 Z M10.5 5.5 V2.5 H2.5 V10.5 H5.5`; `mic` `<rect x="5.5" y="1.5" width="5" height="8" rx="2.5"/><path d="M3.5 7.5 V10.5 H12.5 V7.5 M8 10.5 V13.5 M5.5 13.5 H10.5"/>`; `mic-off` = mic plus `M2.5 13.5 L13.5 2.5`. Die pips are 2px ink squares drawn by `DieFace`, not circles. The ConnectionMark and the on-plan zone arrow are drawn by their components/renderer, not by Glyph.

**Lint scripts** (node, no grep dependency, both Session 1) — `scripts/lint-slop.mjs` scans `apps/web-console/src`, `apps/web-console/index.html`, `apps/web-console/tailwind.config.js` for `linear-gradient|radial-gradient|conic-gradient|backdrop-filter|box-shadow|rgba\(|hsla?\(|#[0-9a-fA-F]{8}\b|shadow-|drop-shadow|ring-|blur-|rounded-(sm|md|lg|xl|2xl|3xl)|animate-|animate-pulse|scale-|transition-all|opacity-(?!0\b|100\b)|opacity:\s*0?\.\d|createLinearGradient|createRadialGradient|createConicGradient|shadowBlur|shadowColor|globalAlpha|ctx\.filter|\bfilter:|text-ink-3|placeholder-ink-3` and fails on any hit, printing `file:line`. `scripts/lint-ip.mjs` **walks the repo root recursively** (skipping `node_modules`, `.git`, `dist`, `__pycache__`, `docs/screens`, `package-lock.json` and its own file) over text extensions `.ts .tsx .js .mjs .cjs .json .md .py .sql .yml .yaml .html .css .txt .env .example`, escaping every list entry with `s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')` before joining into one `\b(…)\b` regex with flags `gi`. Word list (GW-owned proper nouns and product names only — ordinary English words such as "chaos", "imperial", "plasma", "terminator", "rhino", "parchment", "rend" and "overwatch" are deliberately absent, and the game's own name is allowed): imperium, xenos, adeptus, astartes, aquila, space marine, primaris, custodes, mechanicus, intercessor, hellblaster, guardsmen, cadian, ultramarine, ork, boyz, grot, gretchin, squig, eldar, aeldari, drukhari, tyranid, genestealer, necron, votann, t'au, skitarii, commissar, bolter, lasgun, lascannon, chainsword, crozius, deny the witch, combat_patrol, strike_force, kill team, warcry, necromunda, age of sigmar, horus heresy. Prints `file:line`. Wired as `prebuild` in the console (lint-slop) and `lint:ip` / `lint:slop` at root; both run before the final commit of each session. Known hit categories at `26f9cb3` (run the script for the authoritative list; do not work from memory): the game-size enum in `packages/shared-types/src/entities.ts:8–13` and `events.ts:27–32` plus every `combat_patrol`/`strike_force` test literal (3.1 renames them); the squad and vehicle names in `services/voice-service/src/intent-parser.test.ts` and the comments in `disambiguation.ts` (rename squads to Line Section A/B, the vehicle to Carrier, the stratagem to `covering smoke`); `devdata/mock-units/space_marines.json` and any doc that cites it (section 4 deletes it); unit and faction names inside `docs/*.md`, `EXAMPLES.md` and `SERVICES_SUMMARY.md` (replace with the section 4 roster names; the product name Helm and the game's name stay); the `Intercessors` sentence in `CLAUDE.md`'s Testing section; and in Session 2 the console's `tailwind.config.js`, `index.html`, `index.css` theme block, `src/types/index.ts` (47, 74), `canvas-renderer.ts` (279–281) and the App/UnitPanel/VoiceIndicator class strings, all of which section 5 and step 5 replace anyway.

**Anti-slop checklist** (re-read before each screen): no gradient, blur, shadow, alpha fill, ring, drop-shadow, filter, scale or opacity fade — in CSS, Tailwind or canvas; no cards or tiles — regions are ruled cells; square corners except tokens and the focus ring; no emoji, no icon font — every glyph is one of the eight in `Glyph.tsx`; three faces with a semantic reason per mix; state never by colour alone (word + hatch + weight + construction; A solid, B hatched); ink-3 never for text, full stop; buttons are words with a 1px underline rule (`TextButton`), the only filled rectangle is the active phase station; nothing floats, no modals, and nothing changes the grid's size; canvas paints only the twelve theme hexes at alpha 1; 1px canvas strokes snapped via `snapHalfPixel`; no hover translate/scale, no mount fades, no pulsing.

## 6. Screens and components (Session 2)

All in `apps/web-console/src/components`, libs in `src/lib`, one store `src/store/sheet-store.ts` with selectors (never bare `useSheetStore()`).

**Grid** — 1440x900: one CSS grid, columns `[1fr 384px]`, rows `[44px 1fr 132px]`, zero gap, `1px rule` borders on cells. Row 1 spans both columns (PhaseRail). Row 2: FieldMap left, SheetColumn right. Row 3 spans both columns and is a **fixed 132px band**: a 96px VerdictBar cell over a 36px CommandBar cell, separated by a 1px rule. **Nothing on the page ever changes the grid's size**; the FieldMap's ResizeObserver fires only on window resize and the sub-900px drawer, never on verdict state. 1024x768: columns `[1fr 320px]`, same rows.

**Lobby (`Lobby.tsx`)** — Purpose: open or create a sheet. Layout: single ruled form on paper, 640px max, left-aligned, at both breakpoints: heading `Helm — Referee Sheet` (display 28), mono match-id input + `Open sheet` TextButton, `New demo sheet` TextButton, ruled list of recent sheets from `GET /api/matches` (id8, `R{round} / {phase}`, players, updatedAt). Data: `GET /api/matches`, `POST /api/matches/demo`, `?match=` query param auto-opens. States: loading (mono `fetching…`), empty (`no sheets yet`), error (`gateway unreachable at {VITE_API_URL}` in ink-2, retry link), unknown id (`no sheet {id}` from the WS error). Keyboard: Enter opens, `n` new demo.

**PhaseRail (`PhaseRail.tsx`)** — Row 1, 44px, spans both columns; geometry is fixed so two builds look the same. Left cell 44px wide: `ConnectionMark`, a 10px circle with a 1px ink stroke centred in the cell; filled ink when `store.connected`; hatch-hazard inside the circle plus mono `×3` attempt count to its right while reconnecting (the cell widens to fit); mono `bad state` note when zod rejects a `state_update`. Stations: seven cells of 96px (72px under 1100px) starting at x=44 on a 1px ink baseline at y=32.5 spanning the seven cells; 6px major ticks downward (y 32→38) at all eight cell boundaries; 3px minor ticks at cell midpoints. Active station = ink block filling its cell from y=4 to y=32 (28px) with the label in paper, display 15/600 uppercase centred; past stations = ink-2 500 label centred in the same band with a 2px solid tick at the cell's left boundary; future stations = ink-2 500 label with the `tick-hollow` glyph centred on the baseline at the cell midpoint in place of the minor tick (labels full above 1100px, `PRE CMD MOV SHT CHG FGT MOR` below). Interlock note: mono 13px ink-2, single line, ellipsised, left-aligned 16px after the seventh cell, vertically centred in the y 4–32 band, hidden when empty. Right, with 16px right padding and 24px between them: mono `PhaseClock` (`movement 02:41`, from `lib/phase-clock.ts` timestamps per `state_update`) then `RoundStamp` `R1 / A` display 700 28px. Data: `state.phase`, `state.round`, `state.activePlayerId`. Keyboard: `]` next (`[` previous is cut-first, see 9), `Sheet` toggle under 900px. Signature: the interlock (`lib/phase-interlock.ts`, pure) returns `{blocked, reason, note}`; **only two conditions block**: `pre_game` with the survey uncommitted (`Survey not committed`) and a standing STOP (`STOP standing`); `movement` with N active-side placed units that have `!hasMoved` yields `note: 'N units unmoved'` and **does not block**. Clear → `POST /advance-phase {}` and the block slides 240ms; blocked → VerdictBar `advance-form` state pre-labelled with the reason; countersign posts `{override:{reason, initials}}`. Disabled while `verdict.kind==='stop'`.

**FieldMap (`FieldMap.tsx` + `lib/canvas-renderer.ts`)** — Row 2 col 1. Canvas sized by ResizeObserver, `ctx.scale(dpr)`, first draw gated on `document.fonts.ready`; 60x44 plan fit to width inside a 24px paper-2 margin (~16.4 px/in at 1440, ~11.7 at 1024 with numerals every 12in). Keep `tableToScreen/screenToTable`; rewrite every stroke/fill: `renderPlanBorder` (inch ticks, numerals every 6in, bold active deployment edge labelled `A EDGE`/`B EDGE`, 2px stop rule), `renderGrid` (ink-3), `renderDeploymentZones` (zone hatch from `state.deploymentZones`, fallback y 0–12 / 32–44), `renderTerrain` (ink-2 footprint outline, hatch when `blocksLos`, `name` label in display 14 ink-2), `renderObjectives` (hollow 1.5in-radius circle, `label` numeral, ink fill of the numeral only when `controlledBy`), `renderTokens`, `renderRangeContours`, `renderTapeMeasure`, `renderGhostPath`, `renderScanLine`. **Token geometry**: diameter `D = max(1.25in × pxPerInch, 22px)` (22px at both fit scales; grows with zoom); circle for `infantry`, `D`-sided square for `vehicle`, flat-top hexagon of circumscribed diameter `D` for `large`; A = side-a fill, 1px ink outline, letter in paper; B = paper fill, `hatchPattern(side-b, 3)` clipped to the shape, 1.5px side-b outline, letter in ink; letter = display 700 14px, clamped, centred with `textBaseline:'middle'`; selected = four 2px `focus` ticks, 6px long, at N/E/S/W starting 3px outside the outline (the registration crosshair); hover = 1px ink-2 ring 2px outside the outline; pending = outline dashed 3/3 and no fill; null position → drawn in the roster only. Tokens are drawn in roster order and never nudged (positions are truth); the hit-test picks the nearest centre within 0.5in. Data: `state.players[].army.units/profiles`, `state.terrain`, `state.objectives`, store `selectedId`, `hoverId`, `draft`, `pending`, `verdict`, `survey`. Pointer: left-drag pan, cursor-anchored wheel zoom 0.5–4x of fit, click selects, drag-to-place only in survey mode (snap 0.5in). Keyboard: Escape deselects, `f` refits. Overlay: `CoordinateReadout` (mono, `x 24.5  y 18.0`, `d 7.2`, `nearest B4 14.2`, fixed `ch` widths 7/7/6/14) bottom-left; `MapTitleBlock` bottom-right (match id8; `60 × 44 in  1:{scale}`; sheet hash = sha256 of `${lastEvent.id}:${lastEvent.sequence}`, initials, `OVR {n}`), one line on tablet. States: pre_game linework in ink-3 (labels ink-2); STOP → stop rule; disconnected → `ConnectionMark` only, map unchanged. Signature: select → crosshair, ruler-dashed movement ring (1in on / 0.5in off) with `M 6` label on a 2px-padded paper knock-out at 12 o'clock, weapon rings `R 24` at 3 o'clock stacked outward 16px, 160ms sweep from 12 o'clock under rAF; hover another token → 1px ink-2 dimension line with 4px end ticks, mono distance on a knock-out at the midpoint, `IN`/`OUT` against the selected weapon.

**SheetColumn (`SheetColumn.tsx`)** — Row 2 col 2, 384px (320 on tablet). Ruled tab header `Unit / Ledger / Dice` (`Unit / Log / Dice` on tablet). Desktop: `UnitInspector` top (min 52%), `EventTimeline` below with internal scroll; `DiceLedger` replaces the inspector while a roll is armed/shown; `SurveySheet` replaces both in `pre_game`; `MatchSummary` replaces both when `!state.isActive` (cut-first). Tablet: single tabbed region. Keyboard: `1/2/3` switch tabs.

**UnitInspector** — header `A3  Support Weapon Team`; `ProfileTable` (display label left, tabular value right: M T Sv W Ld OC, models `3/3`, wounds `6/6`); `WeaponTable` (name, R, A, BS/WS, S, AP, D, keywords) — rows have radio semantics: clicking a row selects that weapon for the range ring, the tape-measure `IN/OUT` and the `shoot` command's `weaponIds`; the first ranged row is selected by default; `StatusFlags` as words (`moved  advanced  shot  engaged  shocked`, ink-2 when false, ink when true); `Focus on map` link. Data: unit by `selectedId` + profile via `profileId`. Empty: `select a token or type sel a3`. Missing profile: stats row shows `— (no profile)`.

**CommandBar (`CommandBar.tsx` + `lib/command-parser.ts` + `lib/intent-to-command.ts`)** — Row 3 bottom cell, 36px: `prompt` glyph, mono input with history, `mic`/`mic-off` glyph, `VoiceMeter` (six 4×12px rectangles with 1px ink outlines at 2px gaps; lit segments fill hazard). Grammar (`command-parser.ts`, pure, takes a `labels` map and the table size): `move|advance|fall back <label> to <x>,<y>` (moveType normal/advance/fallback); `shoot <target> with <unit>` (DeclareAttack with the selected or first ranged weapon); `roll <n>d6 [to hit|to wound|to save|charge|battleshock] [<t>+]` — `purpose` defaults to `'hit'` and `targetValue` to `4` unless suffixed, because `RollDiceCommandSchema` requires both; `sel <label>`; `use <stratagem> [n cp]`; `score <objective-label> <n>` (ScoreObjective needs the objective's uuid, resolved from `O1`–`O4`); `survey`; `next`; `end match`. Parse-as-you-type into `draft: CommandDraft`; FieldMap ghost-draws moves. Enter builds `{type:'command', command:{id: uuid(), matchId, playerId: activePlayerId, timestamp: ISO, type, ...}}` via `lib/socket.ts`, pushes a pencil row keyed by `command.id` (the gateway echoes it as `command_result.commandId`, 3.4), keeps typing enabled; disabled only while a STOP stands (Enter = withdraw, `O` = override still work). Voice: `useVoice` (kept) → transcript → `POST /voice/api/transcribe {transcript, matchContext:{activeUnits}}` → `intent-to-command` maps `move_unit/declare_attack/roll_dice/advance_phase/select_unit/score_points/undo` to a draft placed in the input (never auto-sent; `requiresConfirmation` shows the prompt in ink-2 above the input). No SpeechRecognition → `mic-off` glyph, mono note `voice unavailable — type commands`. Keyboard: Up/Down history, Escape clears, `D` arms `roll 10d6`.

**VerdictBar + OverrideForm** — Row 3 top cell, **always 96px**; the grid never reflows. States draw a band that grows upward from the input inside the fixed cell: `collapsed` = blank paper, top rule only; `clear` = 44px band at the bottom of the cell, 1px clear hairline on its top edge, `CLEAR` display 36 in clear at the left, citation in Plex Sans beside it (`Movement 6.0 of 6.0 · movement_distance_limit` for MoveUnit, `Range 30.1 of 36 · shooting_range_check` for DeclareAttack, `{ruleId}` otherwise — the numbers are computed client-side from state and the command); collapses on next keystroke; `stop` = 44px hazard-hatch band, `STOP` on a paper knock-out, breach text (`Exceeds M by 1.0 · movement_exceeds_m` from `legalityResult.explanation` and `ruleId`), `Withdraw  Enter` and `Override  O`; the pencil row is struck with the `ovr` glyph in stop, the ghost path re-draws hatched, the plan stop rule appears, the rail is disabled; no close control; `override-form` / `advance-form` = the full 96px: reason (min 12, mono `4 / 12` counter), initials (3 letters), sentence `This override will be entered in the ledger with the sheet hash.`, `Countersign` `HoldButton` (600ms linear underline widen; Ctrl+Enter equivalent), Escape back to `stop`. Band height transitions 240ms within the cell. On countersign → `POST /override {legalityResultId, reason, initials}`; row re-inks `OVR`, title block count increments. Data: `command_result.legalityResult` (`isLegal`, `ruleId`, `explanation`, `suggestedFix`, `id`). Error: gateway 4xx/5xx → band stays, mono error line in ink-2.

**DiceLedger** — header `ROLL 0001  seed —  ARMED` / `ROLL 0001  seed 1f9a…c2  ROLLED` mono (the seed is server-side and appears only after the roll); `Roll` TextButton (Enter); `DieFace` 18px ink-outlined squares with 2px square pips; `TallyRow` `1:2  2:1 …`; hash line `sha256 7e31…9d  verified` + `copy` glyph, `MISMATCH` in stop on disagreement, `UNVERIFIED` (ink-2 italic + hazard `HatchSwatch`) when the event carries no seed or hash; `Verify` expands a mono block: generator `AuditableDice LCG (state = parseInt(seed[0:16],16))`, seed, roll index, the exact string hashed `seed + JSON.stringify(dice)`; superseded rolls struck. Arm = local `RollDice` draft with the next index and a ledger row `DICE ARMED 4d6 roll 0001`; Enter sends it; result from `command_result.events` `DiceRolled` (`seed`, `dice`, `hash`); client recomputes `sha256(seed + JSON.stringify(dice))` via `lib/rng.ts` and compares to `event.hash`. Second Enter does nothing until re-armed.

**EventTimeline / TimelineRow / `lib/diff-events.ts`** — windowed 300 rows; sources merged by timestamp: local (pencil, `DICE ARMED`, `STOP withdrawn`), `command_result` (re-ink by `commandId`), `event` messages, the `GET /api/matches/:id/events` backfill on open, and `diffEvents(prev, next)` on every `state_update` (`unit A3 moved 21,14 -> 24,18`, `PHASE movement -> shooting  04:12`, `SCORE A 6 -> 8`, `ROUND 2`) with ids `diff:<kind>:<key>:<updatedAt>`; server events with matching semantics replace diff rows. Row grid columns `9ch 2ch 6ch 1fr 8ch`: mono timestamp `hh:mm:ss`, side tick, display kind glyph (`CMD ROLL PHASE OVR SURVEY`), body text, short hash; pencil, struck, expanded variants; filter row `all / commands / rolls / overrides`. New rows draw their top hairline 160ms. Empty: `ledger opens on the first command`.

**SurveySheet** — two `RosterTable`s (label, name, class, models, placed tick), `registered 0 / 12`, `Survey` and `Commit survey` TextButtons. `survey` → 2.4s focus scan line, crosshair contraction per token, ticks; a token outside its side's `deploymentZones` rect gets a hazard-hatched row and an ink-2 arrow on the plan to the nearest zone edge; dragging (snap 0.5in) is local: the row un-hatches when the drop is inside the zone and the token stays pencil. Commit enabled only when all ticked and none hatched; sends **one `POST /api/matches/:id/deploy`** with every unit's current position, logs `SURVEY COMMITTED  12 units  sheet …`, tokens ink on the following `state_update`, then advances via the interlock. Escape cancels the scan.

**MatchSummary** (cut-first in Session 2) — shown after `end match` (`POST /api/matches/:id/end`) or round > 5: ruled table VP A/B, objectives held, rolls made, overrides, phases, elapsed, sheet hash; `Copy ledger` (JSON to clipboard) and `Open lobby` text actions. Keyboard: Escape → lobby.

**Primitives** — `Rule` (horizontal/vertical 1px `rule` hairline; `weight="2"` variant in ink-2 for PHASE rows). `TextButton`: display 600 uppercase 15px, underline = 1px ink rule at 3px offset; hover 2px; active (pressed) text ink-2; disabled = ink-2 text with a 1px strike-through, no underline, `aria-disabled="true"`; secondary variant = Plex Sans 14 with a 1px ink-2 underline. `HoldButton`: a TextButton whose underline widens 0→100% over 600ms linear while held (pointer or Space), releasing early cancels; Ctrl+Enter equivalent required. `Kbd`: 13px mono in a 1px `rule` box, 20px tall, 0 4px padding, square. `Glyph` (section 5 set). `HatchSwatch`: 12×12 span with `.hatch-hazard` or `.hatch-zone`.

## 7. Wiring (Session 2)

- `src/lib/env.ts`: `API_URL = import.meta.env.VITE_API_URL ?? 'http://localhost:3000'`, `WS_URL = import.meta.env.VITE_WS_URL ?? 'ws://localhost:3000/ws'`; `apps/web-console/.env.example` with both. Delete `useWebSocket.ts` entirely.
- `vite.config.ts`: the console is `"type":"module"`, so there is no `__dirname` — use `const here = fileURLToPath(new URL('.', import.meta.url))`; `resolve.alias['@helm/shared-types'] = path.resolve(here, '../../packages/shared-types/src/index.ts')`; `server: { port: 5173, proxy: { '/voice': … } }` with **no `open: true`** (it pops a tab at the bare root before `demo.mjs` prints the `?match=` URL); keep `build.sourcemap`.
- Types: delete `src/types/index.ts`; add `"@helm/shared-types": "*"` to the console `package.json`, `"baseUrl": "."` and `"paths": {"@helm/shared-types": ["../../packages/shared-types/src/index.ts"]}` in `tsconfig.json`; import `MatchState, Unit, UnitProfile, MatchCommand, MatchEvent, LegalityResult, PhaseEnum, MatchStateSchema, ServerMessageSchema` from it. Console-local types only for `CommandDraft`, `TimelineRow`, `VerdictState`, `SurveyState`.
- `src/lib/socket.ts`: one module-level WebSocket owned by the store (`connect(matchId)`, `send(msg)`, `close()`); on open → `subscribe`, then `GET /api/matches/:id/events` to backfill the ledger and sheet hash; on message → `ServerMessageSchema.safeParse`, then `state_update` → `MatchStateSchema.safeParse` → `store.applyState(state)` (keeps `prevState`, runs `diffEvents`, updates `phaseClock`); `command_result` → `store.resolveCommand(msg)` finds the pending row by `commandId` (the client's own uuid), re-inks or strikes it, sets `verdict`, merges `events`; `event` → merge; `error` → surface. Reconnect: 1s doubling to 8s, unlimited, resubscribe on open, then `GET /api/matches/:id` to resync. Called once from `App.tsx`.
- Optimistic vs confirmed: state is never mutated locally. Pending work lives in `draft`/`pending`/ghost drawings until `command_result` and the following `state_update`; the ghost token clears when the unit's confirmed position equals the destination or the command resolves illegal/withdrawn.
- ids: `crypto.randomUUID()` with the `lib/rng.ts` v4 fallback for plain-http LAN. `lib/rng.ts` = `sha256(text)` (`crypto.subtle` when available, bundled ~60-line pure-JS SHA-256 otherwise, exported separately so tests can force the fallback) and `uuidv4()`; there is no client-side generator (the server rolls), so no mulberry32.

## 8. Quality gates

- `npm run typecheck` and `npm test` green in all 7 workspaces at every commit. Session 1 test files: `packages/shared-types/src/ws.test.ts`, `services/rules-engine/src/default-rules.test.ts`, `rules-engine.test.ts` (characterization updated), `services/api-gateway/src/state-engine.test.ts` (rewritten sections per 3.2/3.6), `routes.test.ts` (lifecycle rewritten per 3.7), `websocket.test.ts`. Session 2 adds `apps/web-console/src/lib/{command-parser,diff-events,rng,phase-interlock,sheet-store}.test.ts` (add the console `test` script by copying `services/rules-engine/package.json`'s verbatim and `tsx` at the same version; keep tests DOM-free) with at least these cases: **command-parser** — `move a1 to 20,14` → `{type:'MoveUnit', label:'A1', moveType:'normal', destination:{x:20,y:14}}`; `advance b3 to 30,44` → `advance`, `fall back a2 to 1,1` → `fallback`; `shoot b1 with a3` → `{type:'DeclareAttack', attacker:'A3', defender:'B1'}`; `roll 4d6` → `{diceCount:4, purpose:'hit', targetValue:4}` and `roll 3d6 to wound 3+` → `wound`, `3`; `score o1 5` → `{label:'O1', points:5}`; `sel a3`, `survey`, `next`, `end match` → their kinds; `move a9 to 1,1` → `{ok:false, error:'no unit A9'}`; `move a1 to 61,3` → `off table`. **diff-events** — moved, phase, score and round rows with ids `diff:moved:<unitId>:<updatedAt>`, `diff:phase:<to>:<updatedAt>`, `diff:score:<playerId>:<updatedAt>`, `diff:round:<n>:<updatedAt>` and the stated texts; identical states → `[]`; `prev === null` → `[]`. **rng** — pure-JS `sha256('abc')` = `ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad`; the fallback agrees with `node:crypto` on `seed + JSON.stringify([4,4,1,6])`; `uuidv4()` matches `/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/`. **phase-interlock** — `pre_game` + uncommitted survey → `{blocked:true, reason:'Survey not committed'}`; standing STOP → `{blocked:true, reason:'STOP standing'}`; `movement` with 2 unmoved active-side units → `{blocked:false, note:'2 units unmoved'}`; all moved → `{blocked:false, note:null}`. **sheet-store** — `applyState(next)` keeps `prevState` and appends the diff rows; `pushPending` then `resolveCommand({commandId, legalityResult:{isLegal:true}})` re-inks that row and sets `verdict.kind 'clear'`; illegal → row `struck`, `verdict.kind 'stop'` with the legality id; `withdraw()` collapses the verdict and appends `STOP withdrawn`; 301 pushed rows keep the last 300.
- `node scripts/lint-ip.mjs` and `node scripts/lint-slop.mjs` exit 0 (Session 1 for lint-ip; both at every Session 2 commit).
- Session 1 acceptance: `node scripts/demo-smoke.mjs` prints `PASS` for every step.
- Manual QA (Session 2, required): run `devdata/demo/first-five-minutes.md` in Chrome at 1440x900; every step's expected verdict/row appears; a `Ctrl+R` mid-match resyncs via `?match=`; killing and restarting the gateway shows the hatched ConnectionMark then `no sheet` (in-memory). Focus ring visible on every control you touch during the run; `prefers-reduced-motion` emulated in DevTools for one pass with no sweeps or slides.
- Manual QA (Session 2, cut-if-short, in this order): Firefox run (known gap — not installed on this host), 4.5:1 contrast audit of every text/ground pair, 1024x768 run plus `10-tablet.png`, complete keyboard-only run of the script (Tab order: rail → map → sheet → command), MatchSummary + `end match` + `09-summary.png`.
- Screenshots (1440x900 PNG) under `docs/screens/`, required: `01-lobby.png`, `02-survey.png`, `03-field-selected.png`, `04-clear.png`, `05-stop.png`, `06-override.png`, `07-dice.png`, `08-ledger.png`; optional with their features: `09-summary.png`, `10-tablet.png` — via the Browser pane tooling or `chrome --headless=new --screenshot`.

## 9. Order of work and commit plan (two sessions, hard checkpoint)

**Session 1 — backend, fixtures, gates (100%)**
1. (25%) Schema + WS pin + fixtures: 3.1 with every literal edit it lists, section 4 files, delete old devdata and its three directories. Commit.
2. (40%) Gateway: 3.2, 3.4–3.7 with tests, `demo.mjs`, `demo-smoke.mjs`. Commit per subsection.
3. (20%) Rules: 3.3 with the characterization update and `default-rules.test.ts`. Commit.
4. (15%) Lint scripts + repo-wide IP sweep (root markdown, `docs/`, `scripts/init-db.sql`, `CLAUDE.md` updates per section 2(b), test names, package keywords). Commit. (The enum rename already happened in step 1; do not repeat it here.)
Checkpoint: `npm run typecheck`, `npm test`, `node scripts/lint-ip.mjs`, `node scripts/lint-slop.mjs` (trivially green — the console is untouched) and `node scripts/demo-smoke.mjs` all green; `npm run demo` prints `OPEN http://localhost:5173/?match=<id>` (the page still shows the old console — that is expected). Write the Session 1 Final report. Stop.

Cut if short in Session 1, in this order: `POST /end`, `GET /api/matches` listing, `player1Name/player2Name`, `docs/*.md` prose polish beyond removing hits (a hit may be replaced by a roster name mechanically). Never cut: green gates, the whole-repo IP walk at 0, client-owned command ids, `/deploy`, `DiceRolled.hash`, the five new rules with their tests, `demo-smoke.mjs` passing.

**Session 2 — console (100%)**
5. (15%) Console plumbing: shared types, env, `vite.config.ts` per section 7, single socket, store, zod guards; delete `useWebSocket.ts`, `types/index.ts`, `PhaseGuide`, `DiceRoller`, `OverrideModal`, `MatchHeader`, the four console docs (replace with a 30-line README). Commit.
6. (10%) Theme lockdown: globals.css, tailwind, fonts, primitives (Rule, TextButton, HoldButton, Kbd, Glyph, HatchSwatch), lint-slop wired as `prebuild`. Commit.
7. (25%) FieldMap DPR/ResizeObserver + ink re-skin + token geometry + contours + tape. Commit.
8. (20%) PhaseRail geometry + interlock + PhaseClock; CommandBar + pencil rows; VerdictBar fixed cell + OverrideForm. Commit.
9. (15%) DiceLedger + rng/sha256; EventTimeline + diffEvents + events backfill; Lobby. Commit.
10. (15%) SurveySheet with scan, drag and `/deploy` commit; QA script run in Chrome; screenshots; final report. Commit.

Cut if short in Session 2, in this order: Firefox run, contrast audit, 1024x768 QA + `10-tablet.png`, keyboard-only full run, MatchSummary + `09-summary.png`, sub-900px drawer, Verify block recomputation UI, label collision stacking beyond 16px, server-event dedupe against diff rows, `[` previous-phase (would need a gateway route; do not fake it), voice-service round trip (keep the typed path and the fallback note). Never cut: green gates, lint-ip and lint-slop at 0, single socket, envelope/shared types, working STOP + withdraw + override, DICE armed→rolled with the browser-side hash check, `npm run demo` opening straight into the demo sheet, DPR-crisp canvas, the fixed 132px command band.

## 10. Definition of done

**Session 1**
- [ ] `npm run typecheck` exit 0 in all 7 workspaces; `npm test` all pass (state the count); `ws.test.ts`, `default-rules.test.ts`, `websocket.test.ts` exist and the rewritten lifecycle/override tests replace the superseded ones.
- [ ] `node scripts/lint-ip.mjs` (whole-repo walk) and `node scripts/lint-slop.mjs` exit 0; `CLAUDE.md` rule count (24), test count and demo command updated.
- [ ] `npm run demo` prints `OPEN http://localhost:5173/?match=<id>`; `node scripts/demo-smoke.mjs` prints `PASS` for every step, including `movement_exceeds_m` blocking the 7" move, `/override` re-applying it, `DiceRolled` carrying `seed` and `hash` with the hash recomputed equal, and `round 2 / side-a` after ten advances.
- [ ] Fixtures parse with the shared schemas; the demo seed has 12 units, 12 profiles, 4 objectives, 11 terrain pieces, two deployment zones, B5 at (10,30).

**Session 2**
- [ ] `npm install && npm run demo` prints a URL; the script in `devdata/demo/first-five-minutes.md` completes in Chrome in under ten minutes with the stated verdicts, rows and stamps (`R1 / A`, `R1 / B`, `R2 / A`).
- [ ] `npm run typecheck` exit 0; `npm test` all pass (state the count); the five console test files exist with the section 8 cases.
- [ ] `node scripts/lint-ip.mjs` and `node scripts/lint-slop.mjs` exit 0.
- [ ] Console imports `@helm/shared-types`; `src/types/index.ts`, `useWebSocket.ts`, the four overlays and four stale docs are deleted; exactly one WebSocket per page; pencil rows resolve by the client's own command id.
- [ ] The browser-computed `sha256(seed + JSON.stringify(dice))` shows `verified` for the demo roll.
- [ ] Canvas is DPR-scaled with half-pixel snapping and paints only the twelve theme hexes at alpha 1; fonts load from the one Google Fonts link; the grid never changes size on verdict state; the anti-slop checklist holds on every screen.
- [ ] Eight required screenshots in `docs/screens/` (ten with the optional two).

**Final report format** (Markdown, one per session, in this order): (1) *Built* — bullet per commit hash and message; (2) *Verified* — each command run with its exact final output line (`typecheck`, `test` counts, both lint scripts, `npm run demo` URL line, `demo-smoke.mjs` step lines in Session 1, the QA script step table with pass/fail in Session 2); (3) *Screens* — list of files in `docs/screens/` (Session 2); (4) *Known gaps* — what was cut from section 9, the Firefox gap, the console Dockerfile, the `services/state-engine` round arithmetic not mirrored, any CLAUDE.md conflict not pre-resolved in section 2, anything not run; (5) *Next session* — the three highest-value follow-ups.